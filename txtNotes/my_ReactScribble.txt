CREATE REACT PROJECT (without TS):
npm create vite@latest <name> --template react
cd <name>

OR

CREATE REACT PROJECT (with TS):
npm create vite@latest <name> --template react-ts
cd <name>

INSTALL DEPENDENCIES:
npm install

--- start: Additional installs needed for my project ---

(FOR MY PROJECT) INSTALL DEPENDENCIES (for use of Redux, Axios, PropTypes… this will update package.json file):
npm install @reduxjs/toolkit react-redux redux-thunk axios formik prop-types react-router-dom yup

FIX TypeScript + PropTypes compatibility issue:
npm install --save-dev @types/prop-types

Install React Datepicker
npm install react-datepicker
npm install @types/react-datepicker --save-dev

--- end: Additional installs needed for my project ---

START DEV SERVER (start local page):
npm run dev